# Editor fixture: live diagnostics

Open this folder with the BatchWeaver language server running (`batchweaver lsp`)
to see a batching-opportunity diagnostic and a code lens above the `for` loop in
`main.go`. Nothing is written to disk.
