# Editor fixture: transformation preview

Open this folder with the BatchWeaver language server running. Use the code lens
"BatchWeaver: preview batch transformation" or the "Preview Transformation"
command to open a read-only preview. Applying a transformation is a separate,
explicit action; this fixture only demonstrates preview.
