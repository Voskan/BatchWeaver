# Beta Demo Script

1. Show the deterministic in-memory scalar example and its N+1 call trace.
2. Run scan and inspect the candidate.
3. Run proof and explain each obligation and conservative rejection boundary.
4. Generate and review the transformation diff without modifying source.
5. Run transformed race tests through the overlay.
6. Show runtime calls saved and semantic shadow verification.
7. Preview the editor action using the local VSIX.
8. Demonstrate materialize, backup, revert, and scalar fallback.
9. End with unsupported adapters, tested matrix, and private security reporting.

Target duration: 5–10 minutes. Use only public example data.
