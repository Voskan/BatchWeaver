# Good First Issue Policy

The project applies `good-first-issue` only to a genuine, bounded task with
relevant files, explicit acceptance criteria, required tests, no hidden
architecture decision, and maintainer availability for review. It is removed if
the scope becomes ambiguous or safety-critical.

No placeholder issue is created to make the project appear active. Beginner
work must improve a real user or maintainer outcome.
