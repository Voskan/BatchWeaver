# v0.1.0-beta.2 Launch Health

Status: **public hotfix beta**. New installations should use beta.2.

Post-publication verification must cover the Go proxy, `go install` version and
doctor output, external library import, release assets, checksums, archives,
VSIX installation, and the documentation site. Absence of reports is not
evidence of stability.
